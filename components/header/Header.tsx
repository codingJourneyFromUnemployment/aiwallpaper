import NavBar from "./NavBar";

const Header = () => {
  return (
    <header className="sticky top-0 z-50 bg-white shadow-lg w-full flex justify-center">
      <NavBar />
    </header>
  );
}

export default Header;